package com.ricky.metalbox.system;

public interface EntitySystem {
    /**
     * Metodo chiamato ad ogni tick della simulazione.
     * Contiene la logica specifica del sistema.
     */
    void update();
}
